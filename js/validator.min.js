class Validator{constructor(form){this.form=form,form.setAttribute("novalidate",!0)}inputEmptyValidator=options=>{this.formValid=!0;try{const form=this.form;if(!form)throw new Error("ورودی تابع نادرست می باشد!");form.addEventListener("submit",this.formSubmit);let requiredInputs=[...form].filter(element=>"INPUT"==element.tagName&&element.hasAttribute("required"));requiredInputs&&requiredInputs.forEach(input=>{const elementHaveRegex=input.hasAttribute("pattern");null!=options&&1==options.regex&&elementHaveRegex&&input.addEventListener("change",this.regexValidator),elementHaveRegex||input.addEventListener("change",this.inputOnChange);let errorElement=document.createElement("span");errorElement.style.color="#d40000",errorElement.id=`${input.id}Error`,input.parentNode.appendChild(errorElement)})}catch{throw new Error("مشکلی به وجود آمده است!")}};formSubmit=e=>{let arrayOfInput=[...e.target];arrayOfInput=arrayOfInput.filter(element=>"INPUT"==element.tagName&&element.hasAttribute("required")),arrayOfInput.forEach(input=>{const elementHaveRegex=input.hasAttribute("pattern");let spanError=document.getElementById(`${input.id}Error`);if(""==input.value){let[label]=input.labels;e.preventDefault(),spanError.innerHTML=`عنوان ${label.textContent} الزامی میباشد.`}else if(spanError.innerHTML="",elementHaveRegex){let regexStatus;this.regexInputOnChange(input)||e.preventDefault()}})};inputOnChange=e=>{let targetInput=e.target,spanError=document.getElementById(`${targetInput.id}Error`);""==targetInput.value?spanError.innerHTML=`عنوان ${targetInput.labels[0].textContent} الزامی میباشد.`:spanError.innerHTML=""};regexInputOnChange=input=>{let pattern=input.pattern,spanError=document.getElementById(`${input.id}Error`),exp,regexResult=new RegExp(pattern).test(input.value);return regexResult?spanError.innerHTML="":""==input.value?spanError.innerHTML=` عنوان ${input.labels[0].textContent} الزامی میباشد.`:spanError.innerHTML=` فرمت ${input.labels[0].textContent} نادرست میباشد.`,regexResult};regexValidator=e=>{let targetInput=e.target;this.regexInputOnChange(targetInput)}}